// inverte a ordem dos itens de um array

function inverterArray(array) {
    const arrayInvertido = array.reverse()
    console.log(array) // altera o original!
    console.log(arrayInvertido)
}

inverterArray(["nos", "vos", "eles"])