// calculando o delta
function baskahara (a,b,c) {
const delta = Math.pow(b,2) -  4*a*c
console.log(delta)
}
    
baskahara(7,3,4) 
    // delta > 0: duas raízes reais e diferentes
    // delta < 0: nao tera raizes reais
    // delta = 0: somente um valor real / dois resultados iguais